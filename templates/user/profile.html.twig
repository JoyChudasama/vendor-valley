{% extends 'base.html.twig' %}

{% block title %}
	{{parent()}}
	|
	{{user.firstName}}
{% endblock %}

{% block stylesheets %}
	{{ parent() }}
{% endblock %}

{% block body %}
	<div class="container my-5 p-3">
		
		<div class="row">
			<div class="col-sm-12 col-md-12 col-lg-12">
				{{include('components/sidebar.html.twig')}}
			</div>
		</div>

		<div class="row">
			<div class="col-sm-12 col-md-12 col-lg-12" {{stimulus_controller('refresh',{refreshUrl:path('app_user_edit',{'id':user.id} )})}}>
				<div data-refresh-target="refreshContent">
					{{include('user/_edit_form.html.twig',{form:form})}}
				</div>
			</div>
		</div>

	</div>

{% endblock %}
